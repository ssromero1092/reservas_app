import 'package:flutter/material.dart';

class KPadding {
  static const allXS = EdgeInsets.all(4);
  static const allSM = EdgeInsets.all(8);
  static const allMD = EdgeInsets.all(16);
  static const horizontalMD = EdgeInsets.symmetric(horizontal: 16);
  static const verticalSM = EdgeInsets.symmetric(vertical: 8);
}
